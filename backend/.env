DEBUG=True
SECRET_KEY=django-insecure-teacherconnect-dev-key-change-in-production
DATABASE_URL=sqlite:///db.sqlite3
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
FRONTEND_URL=http://localhost:3000

# Email Configuration
# By default, emails are printed to console in development
# For production, configure SMTP settings below:

# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password
# DEFAULT_FROM_EMAIL=TeacherConnect <noreply@teacherconnect.com>

# Set to True to send actual emails even in DEBUG mode
SEND_EMAILS_IN_DEBUG=False

# Cloudflare R2 Storage (S3-compatible)
# Leave empty to use local storage
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=teacherconnect-media
R2_PUBLIC_URL=
